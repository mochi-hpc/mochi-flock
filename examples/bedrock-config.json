{
    "libraries": {
        "flock": "libflock-bedrock-module.so"
    },
    "providers": [
        {
            "type": "flock",
            "name": "my-flock-provider",
            "provider_id": 42,
            "config": {
                "group": {
                    "type": "dummy",
                    "config": {}
                }
            }
        }
    ]
}
